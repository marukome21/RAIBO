<h1>投稿一覧です！</h1>

<h1>ログアウトはこちらです！</h1>
<%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
<br>

<h2>POST</h2>
  <% @posts.each do |posts| %>
    <%= link_to post_path(posts.id) do %><%= posts.post_text %><% end %><br>
    <% if posts.image.attached? %>
      <%= image_tag posts.image, size: "600x400" %><br>
    <%else%>

    <%end%>
  <% end %>