<h2>POST</h2>
  <table class="table table-hover table-inverse">
    <tbody>
        <tr>
          <td><%= @post.post_text %></td>
          <td><%= link_to "削除", post_path(@post.id), method: :delete, class:"btn btn-sm btn-danger", data: {confirm: "本当に消しますか？"} %></td>
        </tr>
    </tbody>
  </table>