<br>
<h1><%= link_to "⇚", admin_root_path %>  ユーザー一覧</h1>

<div class="container pt-5">
  <div class="row">
    <div class="col-md-9 mb-3">
      <h3 class="px-4 text-center"><%= @user.user_name %>さんの詳細</h3>
    </div>
  </div>
    <div class="pl-5">
      <p class="my-3"><strong>会員ID </strong><span style="margin-left:150px;"><%= @user.id %></span></p>
      <p class="my-3"><strong>ユーザー名 </strong><span style="margin-left:110px;"><%= @user.user_name %></span></p>
      <p class="my-3"><strong>プロフィール </strong><span style="margin-left:100px;"><%= @user.profile %></span></p>
      <p class="my-3"><strong>メールアドレス </strong><span style="margin-left:80px;"><%= @user.email %></span></p>
      <p class="my-3"><strong>会員ステータス </strong><span style="margin-left:90px; color:<%= @user.is_active ? 'green' : 'gray' %>;"><%= @user.is_active ? '有効' : '退会済み' %></span></p>

     <%= link_to '編集する', edit_admin_user_path(@user), class: 'btn btn-success btn-space' %>
    </div>
  </div>
</div>

<br>
<h2 class="text-center">POST一覧</h2>
<div class="card-list-container pbs-5">
  <% @posts.each do |posts| %>
    <div class="card w-50 mx-auto mt-4 shadow-lg">
      <div class="card-body">
        <div class="card-title">
          <div class="row">
            <div class="col-10">
              <%= image_tag posts.user.get_user_image(100,100), class: "mt-3 rounded-circle img-thumbnail"; %>
              <strong><%= posts.user.user_name %></strong>
            </div>
            <div class="text-right pt-4">
              <%= link_to "削除", admin_user_path(posts.id), method: :delete, class:"btn btn-sm btn-danger", data: {confirm: "本当に消しますか？"} %>
            </div>
          </div>
          <%= link_to post_path(posts.id) do %>
            <div class="row py-5">
              <div class="card-text pl-3">
                <%= posts.post_text %>
              </div>
            </div>
            <div class="row">
              <% if posts.image.attached? %>
                <%= image_tag posts.image, class: "card-img-top img-thumbnail"; %>
              <%else%>
              <%end%>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>